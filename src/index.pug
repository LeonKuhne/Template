doctype html
html(lang='en')

  head
    // TEMP
    //meta(http-equiv="refresh", content="5")

    // import third party
    link(href="https://cdn.jsdelivr.net/npm/vuetify@2.x/dist/vuetify.min.css", rel="stylesheet")
    script(src="https://cdn.jsdelivr.net/npm/vue@2.6.12")
    script(src="https://cdn.jsdelivr.net/npm/vuetify@2.x/dist/vuetify.js")

    // import custom
    link(rel="stylesheet" href="styles.css")
    script(src="script.js")

  body
    v-app#app
      title {{ title }}
      #story
        h2.info STORY
        v-textarea(placeholder="On a bright summers day...") {{ title }}
        div.action
          button.error RESET
          button.primary SAVE
